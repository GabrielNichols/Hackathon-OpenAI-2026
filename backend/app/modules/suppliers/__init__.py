"""Supplier intelligence bounded context.

The package intentionally does not register itself in a central application router. Consumers
import the feature router or the directory adapter from their public modules.
"""

